import { TextAreaResizeDirective } from './text-area-resize.directive';

describe('TextAreaResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new TextAreaResizeDirective();
    expect(directive).toBeTruthy();
  });
});
